{% comment %}

This snippet renders the relative map url for a given document in a map item.

Args:
  map: the collection map item

Renders:
  relative map url

{% endcomment %}
{%- include getParent.liquid file_path=include.map.url -%}
{{ gd_directory_path | relative_url }}