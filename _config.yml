collections:
    maps:
        output: true
        permalink: /:name

defaults:
    - 
      scope:
        path: ""
        type: "maps"
      values:
        layout: "maps"
    - 
      scope:
        path: "gitzip/js"
      values:
        js: true

baseurl: "/CommunityMaps"
